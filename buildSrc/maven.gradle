plugins {
    id 'java-library'
    id 'maven-publish'
}

buildscript {

    publishing {
        publications {
            mavenJava(MavenPublication) {
                from components.java
            }
        }
    }
}

